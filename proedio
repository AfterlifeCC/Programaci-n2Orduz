#include <stdio.h>

int main() {

int grades[20]={[2]=10,[1]=2, [0]=1};
int count=20, i, sum=0;
float average=0;
for(i = 2; i< count; ++i){
printf("%d\t", i+1);
scanf("%d", &grades[i]);
sum+=grades[i];
}
average=(float)sum/count;
printf("El promedio es: %f\n",average);

return 0;
}
